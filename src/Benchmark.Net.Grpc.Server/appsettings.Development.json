{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
      //"Microsoft.AspNetCore": "Information" // put this on Warning, or remove totally to disable every request logging (faster performance wor server)
    }
  }
}
